import 'package:flutter/material.dart';

class PantallaDeIngresoDeMateriasPrimas extends StatelessWidget {
  const PantallaDeIngresoDeMateriasPrimas({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Ingreso de Materias Primas')),
      body: const Center(child: Text('Pantalla de Ingreso de Materias Primas')),
    );
  }
}
